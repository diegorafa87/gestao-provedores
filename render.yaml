services:
  # Frontend React
  - type: web
    name: frontend-gestao-provedores
    env: node
    region: oregon
    plan: free
    buildCommand: cd frontend && npm install && npm run build
    startCommand: cd frontend && node server.js
    envVars:
      - key: PORT
        value: 3000
      - key: NODE_ENV
        value: production

  # Backend Node.js
  - type: web
    name: backend-gestao-provedores
    env: node
    region: oregon
    plan: free
    buildCommand: cd backend && npm install
    startCommand: cd backend && node src/index.js
    envVars:
      - key: MONGODB_URI
        value: ${MONGODB_URI}
      - key: PORT
        value: 5001
      - key: NODE_ENV
        value: production
